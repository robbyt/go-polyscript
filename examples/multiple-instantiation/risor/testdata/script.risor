// Script has access to ctx variable passed from Go
let name = ctx["name"]
let message = "Hello, " + name + "!"

// Return a map with our result
{
    "greeting": message,
    "length": len(message)
}
